# Lines starting with # are comments

# prefer best format and merge
#-f bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best
# New format syntax
-f bestvideo*+bestaudio/best
--merge-output-format mkv


# Audio only mode
#-x

# Do not copy the mtime
--no-mtime

# Use this proxy
#--proxy 127.0.0.1:3128

# Saves video in current working directory under a folder of the same name preceded by upload date in YYYYMMDD
-o ./%(upload_date)s-%(title)s/%(title)s.%(ext)s

# Alternative option that does not make a folder
#-o %(title)s.%(ext)s

# write desc to .description file
--write-description

# uses some unicode alternatives for chars like ? : which can cause issues especially with windows
--windows-filenames

# embed thumbnail in video and write it seperately to disk
--write-thumbnail

--embed-thumbnail 

# metadata and web link to original video
--add-metadata  

--write-link

# for live recording stuff, waits for stream to start and dls from the beginning
--wait-for-video 30
--live-from-start

# yes this is google chrome yup
--impersonate Chrome

#cookies
# Disabled due to issues with yt blocking accounts
#--cookies-from-browser firefox

# dl subtitles and / or live chat
--sub-langs all

# set higher retry count
--retries 30
--file-access-retries 7
--fragment-retries 30

# archive settings
#--download-archive archive.txt
